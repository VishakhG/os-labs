To compile just run 'make'.

To run call the object file 'linker' and a file name as a command line argument.
For example './linker "/path/to/file".

The source file is named: '2passlinker.cpp'
The executable is called: 'linker'

The directory structure is :

2pass-linker
	|
	2passlinker.cpp makefile README.txt
